[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "orange-disease-detector"
version = "1.0.0"
description = "Flask-based Orange Disease Detector"
requires-python = ">=3.10"

[tool.vercel]
entrypoint = "backend.app:app"
